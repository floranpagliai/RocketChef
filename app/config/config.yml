imports:
    - { resource: parameters.yml }
    - { resource: security.yml }

framework:
    #esi:             ~
    translator:      { fallback: "%locale%" }
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    templating:
        engines: ['twig']
        #assets_version: SomeVersionScheme
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        handler_id: ~
        name: ROCHEFSESSID
    fragments:       ~
    http_method_override: true

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"
    form:
        resources:
            - ShtumiUsefulBundle::fields.html.twig

# Assetic Configuration
assetic:
    use_controller: false
    debug:          "%kernel.debug%"
    bundles:        [ ]
    filters:
        cssrewrite: ~
        less:
            node: "C:\\Program Files (x86)\\nodejs\\node.exe"
            node_paths: ["C:\\Users\\Utilisateur\AppData\\Roaming\\npm\\node_modules"]
            apply_to: "\.less$"
        yui_css:
            jar: "%kernel.root_dir%/Resources/java/yuicompressor.jar"
    assets:
        fontawesome_css:
            inputs:
                - %kernel.root_dir%/../vendor/components/font-awesome/less/font-awesome.less
            filters:
                - less
                - cssrewrite
            output: css/font-awesome.css
        pace_js:
            inputs:
                - %kernel.root_dir%/Resources/js/pace.min.js
            output: js/pace.min.js
        datatables_js:
            inputs:
                - %kernel.root_dir%/Resources/js/jquery.dataTables.min.js
            output: js/jquery.dataTables.min.js
        piechart_js:
            inputs:
                - %kernel.root_dir%/Resources/js/jquery.easypiechart.min.js
            output: js/jquery.easypiechart.min.js
        raphael_js:
            inputs:
                - %kernel.root_dir%/Resources/js/raphael.min.js
            output: js/raphael.min.js
        morris_js:
            inputs:
                - %kernel.root_dir%/Resources/js/morris.min.js
            output: js/morris.min.js


braincrafted_bootstrap:
    less_filter: less
    assets_dir: %kernel.root_dir%/../vendor/twbs/bootstrap
    jquery_path: %kernel.root_dir%/../vendor/jquery/jquery/jquery-1.10.2.js

# Doctrine Configuration
doctrine:
    dbal:
        driver:   "%database_driver%"
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8
        # if using pdo_sqlite as your database driver, add the path in parameters.yml
        # e.g. database_path: "%kernel.root_dir%/data/data.db3"
        # path:     "%database_path%"

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: "%mailer_transport%"
    host:      "%mailer_host%"
    username:  "%mailer_user%"
    password:  "%mailer_password%"
    spool:     { type: memory }

shtumi_useful:
    autocomplete_entities:
        ingredient:
            class: GastroDataBundle:Ingredient
            role: ROLE_USER
            property: name
            search: contains
